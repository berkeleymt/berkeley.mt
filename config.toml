# The URL the site will be built for
base_url = "/"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

taxonomies = [{ name = "tags", render = false }]

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = false

[extra]
# Put all your custom variables here

# Site-wide advisory banner, shown on every page. Comment out or delete the
# whole [extra.advisory] table to remove the banner.
# `status` is one of "info", "warning", or "error"; `body` is markdown.
# `timestamp` is free text shown in the top right; include the timezone.
[extra.advisory]
status = "warning"
title = "ContestDojo registration issues"
timestamp = "Updated September 14, 2026, 10:00 PM PT"
body = """
We are actively troubleshooting the issue on ContestDojo. We will be releasing
an update later tomorrow regarding adjustments to our Phase 1/Phase 2
registration. All students who want to register will be able to.

Next expected update: September 15, 2026 at 9:00 AM PT.
"""
