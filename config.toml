# The URL the site will be built for
base_url = "/"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

taxonomies = [{ name = "tags", render = false }]

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = false

[extra]
# Put all your custom variables here

# Site-wide advisory banner, shown on every page. Comment out or delete the
# whole [extra.advisory] table to remove the banner.
# `status` is one of "info", "warning", or "error"; `body` is markdown.
# `timestamp` is free text shown in the top right; include the timezone.
[extra.advisory]
status = "info"
title = "Hotel block released"
timestamp = "September 22, 2026"
body = """
We have just released a hotel room block with discounted rates around contest weekend 
for teams looking for accommodations.
**The cutoff date for booking is Tuesday, October 27, 2026.**

Please [see here](https://berkeley.mt/events/bmt-2026/#hotels) for details.
"""
